{
  "name": "pageviews",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic fake pageview counter as a browser-global TypeScript plugin.",
  "scripts": {
    "build": "tsc && terser build/pageviews.js --compress --mangle --comments false --output dist/pageviews.min.js"
  },
  "devDependencies": {
    "terser": "^5.44.0",
    "typescript": "^5.9.2"
  }
}